---
title: "Alia"
description: "Multi-surface context-agent platform with autonomous execution, persistent context graph, and policy controls."
order: 0
---

# Alia

Alia is a multi-surface context-agent platform focused on autonomous execution with policy controls. One runtime powers the Expo app, the Codea VS Code extension, the Cowork desktop client, the developer CLI, and every webhook-driven trigger — all on top of a shared autonomy loop and a persistent context graph.

## What it is

A single chat runtime (`/alia/chat` and `/v1/chat/completions`) serves every surface. Each turn runs the autonomy loop `classify → recall → retrieve → act → learn`, draws from a per-user context graph, and is governed by an explicit risk policy (`R0`/`R1`/`R2`/`R3`) so writes are reversible, external impact requires approval, and destructive actions are blocked.

Hosted inference goes through Kaana. Alia exposes canonical `kaana-*` routing profiles and product modes, while Kaana owns provider credentials, deployments, health, retry and fallback. [Model abstraction](./model-abstraction.mdx) describes the boundary, and [ADR 0003](./adr/0003-model-revision-deployment-provider-routing-profile.md) records the vocabulary behind it.

## Architecture

```
                            +-------------------+
                            |   Expo App (Web,  |
     User  ────────────────>|   iOS, Android)   |
                            +---------+---------+
                                      |
                            POST /v1/chat/completions (SSE)
                                      |
                            +---------v---------+
                            |  Express API      |
                            |  (packages/api)   |
                            +--+----------+-----+
                               |          |
              +----------------+          +-----------------+
              |                                             |
     +--------v---------+                       +-----------v-----------+
     |   PostgreSQL     |                       |  Kaana inference      |
     |   (drizzle)      |                       |  data plane           |
     +--------+---------+                       +-----------------------+
     +--------v---------+
     |  Redis (Valkey)  |     Socket.IO (real-time events,
     |  rate limits,    |     approval requests, streaming)
     |  caching         |
     +------------------+
```

PostgreSQL is Alia's product store. The last domains — conversations, messages, agents, organizations, containers, skills and the rest — are ported, and `packages/api` opens no MongoDB connection and registers no Mongoose model. Under [ADR 0001](./adr/0001-alia-oxy-kaana-responsibility-boundary.md), every hosted inference call crosses the published Oxy inference client and Oxy calls Kaana; only a user's explicitly connected local runtime stays outside that hop.

| App | Stack | Purpose |
| --- | --- | --- |
| `packages/app` | Expo + React Native | Main app (web + iOS + Android) |
| `packages/api` | Express + TypeScript | Core API runtime |
| `packages/alia-codea` | VS Code extension | Coding assistant surface |
| `packages/alia-codea-cli` | Bun CLI | Terminal coding assistant |
| `packages/alia-cowork` | Electron | Desktop assistant surface |
| `packages/alia-console` | TanStack Start + React | Developer console |
| `packages/alia-canvas` | Vite + React | Canvas / diagram editor |
| `packages/alia-docker-host` | Express + TypeScript | Sandboxed container host |
| `packages/integrations` | Express + TypeScript | Telegram / WhatsApp / Discord / browser bridges, and the MCP client |
| `packages/alia-chat` | React | Published as `@alia.onl/sdk` |
| `packages/shared-types` | TypeScript | Shared types |

## Install

The monorepo uses Bun workspaces and Turbo. Bun is the only supported package manager.

```sh
bun install
```

Copy `packages/api/.env.example` to `packages/api/.env` and fill it in. `DATABASE_URL` is the only database variable and the API refuses to boot without it. Alia declares no Mongo driver dependency. Hosted provider credentials are not Alia environment variables or database rows; Kaana stores and uses them in its own PostgreSQL database. Alia authenticates to Oxy with its Oxy application service credential, and Oxy is the only hop that calls Kaana.

## Quick start

Boot the full stack:

```sh
bun run dev
```

Or run individual workspaces:

```sh
bun run dev:api          # Express API only
bun run dev:app          # Expo app (web + tunnel)
bun run dev:admin        # Admin console
bun run dev:canvas       # Canvas app
bun run dev:codea        # VS Code extension
bun run dev:cowork       # Electron desktop app
```

Hit the unified chat endpoint:

```ts
const res = await fetch('https://api.alia.onl/v1/chat/completions', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: 'Bearer alia_sk_…',
  },
  body: JSON.stringify({
    model: 'profile:v1',
    messages: [{ role: 'user', content: 'Prepare my meeting with Sarah' }],
    stream: true,
  }),
});
// SSE stream — see "Chat runtime" for the event contract.
```

`alia_sk_*` credentials authenticate `api.alia.onl/v1/*` inside a bounded compatibility window. New integrations register in Oxy Console and call `api.oxy.so/v1` — see [developer access](./developers-portal.md).

## Core concepts

### Autonomy loop

Every chat turn runs the same five-step loop in `packages/api/src/lib/autonomy/runtime.ts`:

1. **classify** — detect intent (`meeting_prep`, `inbox_digest`, `project_status`, `task_followup`, `monitoring`, `research`, `general`)
2. **recall** — load ranked `ContextSource`s and `LearningRule`s from the per-user context graph
3. **retrieve** — pull data from top sources (integrations, MCP servers, Oxy ecosystem services)
4. **act** — produce the answer, call tools, stream the response over SSE
5. **learn** — update source quality scores and persist new `LearningRule`s

The loop is shared verbatim across app, Codea, and Cowork. Feature flags (`AUTONOMY_RUNTIME_ENABLED`, `AUTONOMY_CONTEXT_GRAPH_ENABLED`, `AUTONOMY_APPROVALS_ENABLED`, `AUTONOMY_ROLLBACK_ENABLED`, `AUTONOMY_OXY_EVENTS_ENABLED`) gate each stage and default to enabled.

### Context graph

Persistent autonomy state:

| Table | Tracks | Read through |
|-------|--------|--------------|
| `context_sources` | freshness, precision, average cost, latency, failure rate of a source | `db/autonomy/contextGraphRepository.ts` |
| `context_nodes` | entities discovered during retrieval (people, projects, docs, threads) | `db/autonomy/contextGraphRepository.ts` |
| `context_edges` | relationships and confidence weights between nodes | `db/autonomy/contextGraphRepository.ts` |
| `retrieval_strategies` | per-intent source ordering and fallback paths | `db/autonomy/contextGraphRepository.ts` |
| `learning_rules` | persisted corrections, constraints, and preferences | `db/autonomy/learningRuleRepository.ts` |
| `rollback_records` | before/after state and rollback payload for `R1` writes | `db/agents/rollbackRecordRepository.ts` |

User-visible memory (preferences, long-term facts, occupation, tone) lives in `user_memories` and `user_memory_entries`, exposed via `/memory/*`.

### Risk governance

Every action carries a risk class enforced by the runtime:

| Level | Meaning | Behavior |
|-------|---------|----------|
| `R0` | Read-only | Fully autonomous |
| `R1` | Reversible write | Autonomous + `RollbackRecord` saved |
| `R2` | External / unknown impact | Requires user approval over Socket.IO (`alia.approval_request` / `alia.approval_result`) |
| `R3` | Destructive | Blocked |

Approvals are real-time. The client receives `alia.approval_request` while the stream is paused and resolves it with an `approve` / `deny` decision; timeouts default to 60s.

### Model abstraction

The product exposes modes and canonical `kaana-*` routing profiles. Alia sends
the selected profile or concrete model reference to Oxy without selecting an
upstream deployment itself. Oxy resolves policy; Kaana owns provider credentials, health, routing,
retry and fallback; provider names and endpoints do not appear on the product
surface.

`packages/api/src/internal/providers` and the provider-labelled database tables
remain only for historical data and credential migration. They are not on the
hosted boot or chat path. [Model abstraction](./model-abstraction.mdx) documents
the current boundary and the conditions for removing those retained records.

### Trigger engine

`/automations` is the normalized scheduling and control API. It records explicit actors,
resources, actions, data flow, limits, autonomy and `observe | execute` mode. `/triggers`
remains the transitional API for these legacy trigger types:

- `schedule` — cron, daily, or interval
- `webhook` — token URL with optional HMAC and IP allowlist
- `integration_event` — matched by `service + event + filters`
- `agent_heartbeat` — periodic agent health / status check

Legacy runs write a `trigger_executions` row with status, tool calls, token usage and duration, through `db/automation/triggerRepository.ts`. Structured runs and decisions use `automation_runs` and `automation_steps`. Both row types are reconciled by the same elected scheduler, and legacy trigger actions follow the unified action contract: `{ prompt, agentId?, roleId?, useTools, notify?, channelId? }`.

## Deep dives

- [Chat runtime and SSE events](./chat-runtime.mdx)
- [Kaana model abstraction](./model-abstraction.mdx)
- [The `alia-*` layer: audit before removal](./alias-layer-audit.mdx)
- [Multi-channel integrations and tools](./integrations.mdx)
- [Skills: the Agent Skills format, and what an install authorises](./skills.mdx)
- [Architecture decision records](./adr/README.md)
- [Compatibility window and sunset criteria](./migration/compatibility-window.md)
