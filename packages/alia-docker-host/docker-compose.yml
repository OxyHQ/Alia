services:
  alia-docker-api:
    build: .
    container_name: alia-docker-api
    ports:
      - "9090:9090"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - traefik-dynamic:/etc/traefik/dynamic
    environment:
      - DOCKER_HOST_SECRET=${DOCKER_HOST_SECRET}
      - PREVIEW_DOMAIN=${PREVIEW_DOMAIN:-preview.alia.onl}
      - NODE_ENV=production
      - PORT=9090
    restart: unless-stopped
    networks:
      - alia-containers

  alia-preview-proxy:
    image: traefik:v3.2
    container_name: alia-preview-proxy
    command:
      - --api.insecure=false
      - --providers.docker=true
      - --providers.docker.exposedByDefault=false
      - --providers.docker.network=alia-containers
      - --providers.file.directory=/etc/traefik/dynamic
      - --providers.file.watch=true
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443
      - --certificatesresolvers.letsencrypt.acme.httpchallenge=true
      - --certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web
      - --certificatesresolvers.letsencrypt.acme.email=${ACME_EMAIL:-admin@alia.onl}
      - --certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json
      - --log.level=WARN
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - letsencrypt:/letsencrypt
      - traefik-dynamic:/etc/traefik/dynamic:ro
    restart: unless-stopped
    networks:
      - alia-containers

volumes:
  letsencrypt:
  traefik-dynamic:

networks:
  alia-containers:
    name: alia-containers
    driver: bridge
