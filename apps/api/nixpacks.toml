[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "cd ../.. && npm ci",  # Install from monorepo root
  "cd apps/api"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
